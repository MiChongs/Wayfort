/* eslint-disable */
// Brand mark — shard A. See ./mark for the full SECURITY NOTE. Names are
// intentionally opaque; this is one of several scattered shards and carries
// only the first half of the encoded wordmark plus one key seed. On its own it
// reveals nothing.
export const _s1 = 0x5a3c
export const _e1 = [8200, 7379]
