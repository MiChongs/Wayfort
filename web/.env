# 后端 HTTP 地址（服务器端代理调用，不暴露给浏览器）
BACKEND_HTTP_URL=http://127.0.0.1:18080

# 后端 WebSocket 地址（浏览器直连，必须以 NEXT_PUBLIC_ 开头才会注入到客户端 bundle）
NEXT_PUBLIC_BACKEND_WS_URL=ws://127.0.0.1:18080

# 前端调用基础（保留 /api/proxy 即走 Next.js Route Handler 代理）
NEXT_PUBLIC_API_BASE=/api/proxy/api/v1
